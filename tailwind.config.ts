// Tailwind v4: colores y fuentes definidos via @theme en globals.css
// Este archivo existe para referencia y compatibilidad
const config = {
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
  ],
};

export default config;
